FROM node:18-alpine

WORKDIR /app

# 安装必要的工具
RUN apk add --no-cache bash

# 复制 package.json 和 package-lock.json（如果存在）
COPY package*.json ./

# 安装依赖
RUN npm install

# 暴露端口
EXPOSE 3000

# 启动开发服务器
CMD ["npm", "run", "dev", "--", "--host"]